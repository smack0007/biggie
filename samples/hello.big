import "./person.big"
import greet "./greet.big"

func main(): int {  
  var person: person.Person = { "Buddy", 42 };
  println("Hello %s, you are %d years old!", person.name, person.age);

  return 0;
}
