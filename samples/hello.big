import "./person.big"
import "./greet.big"

func main(): int {  
  var person: Person = { "Buddy", 42 };
  
  sayHello(person, GreetType.Formal);
  sayHello(person, GreetType.Informal);
  sayHello(person, GreetType.Crazy);

  return 0;
}
