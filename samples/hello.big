import personLib "./person.big"
import "./greet.big"

func main(): int {  
  var person: personLib.Person = { "Buddy", 42 };
  
  greet.sayHello(person, greet.GreetType.Formal);
  greet.sayHello(person, greet.GreetType.Informal);
  greet.sayHello(person, greet.GreetType.Crazy);

  return 0;
}
