struct Person {
  name: string;
  age: int;
}

func (person: Person) sayHello(): void {
  println("Hello {}, you are {} years old!", person.name, person.age);
}

func main(): int {  
  var person: Person = { "Buddy", 42 };
  
  person.sayHello();

  return 0;
}
