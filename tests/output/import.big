import person "../shared/person.big"
import greetEn "../shared/en/greet.big"
import greetDe "../shared/de/greet.big"

func main(): int {  
  var person: person.Person = { "Buddy", 42 };
  greetEn.greet(person, 10);
  greetDe.greet(person, 10);
  return 0;
}
