func main(): int {
  var data: []int = [ 1, 2, 3, 4 ];
  
  // TODO: Reimplement data.length
  println("[4][ %d, %d, %d, %d ]", data[0], data[1], data[2], data[3]);
  println("%d %d %d %d", data[0], data[1], data[2], data[3]);

  return 0;
}