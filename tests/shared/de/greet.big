import "../person.big"

export func greet(person: Person, count: int32): void {
  var i: int32 = 0;
  while (i < count) {
    println("Hallo {}, du bist {} Jahre alt!", person.name, person.age);
    i += 1;
  }
}